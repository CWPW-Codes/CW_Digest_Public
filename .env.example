# Copy this file to .env and fill in values as needed.

# Required only when using --ai-summaries
OPENAI_API_KEY=your_openai_api_key_here

# Optional: point to local OAuth Desktop client credentials JSON
# If unset, defaults to ./config/google_oauth_client.json
CW_DIGEST_OAUTH_CLIENT_FILE=./config/google_oauth_client.json

# Optional: override Gmail OAuth token cache location
# If unset, defaults to ~/.config/cw-digest/gmail_token.json
CW_DIGEST_GMAIL_TOKEN_FILE=~/.config/cw-digest/gmail_token.json

# Optional alternative to credentials file:
# GOOGLE_OAUTH_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret
